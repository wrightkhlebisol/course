version: '3.8'

services:
  nlp-processor:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - /tmp:/tmp:ro
    environment:
      - FLASK_ENV=development
      - PORT=5000
    restart: unless-stopped

networks:
  default:
    name: nlp-log-network
