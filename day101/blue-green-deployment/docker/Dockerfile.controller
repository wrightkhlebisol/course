FROM python:3.11-slim

WORKDIR /app

# Copy requirements and install dependencies
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code directly to app directory (not nested)
COPY backend/app/ ./

EXPOSE 8000

CMD ["python", "-m", "main"]
